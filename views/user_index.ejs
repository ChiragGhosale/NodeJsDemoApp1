<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Document</title>
</head>

<body>
    <div class="container" >
    <h1 align="center">
        <%= title %>
    </h1>
    <a href="/add" class="btn btn-sm btn-primary">Add Teacher</a><br/><br />
    <table class="table table-dark table-bordered  w-100">
        <thead class="" align="center">
            <tr class="tr">
                <th class="border-3 border-danger">Sr.No.</th>
                <th class="border-3 border-danger">TeachersId</th>
                <th class="border-3 border-danger">name</th>
                <th class="border-3 border-danger">Qualification</th>
                <th class="border-3 border-danger">Subject</th>
            </tr>
        </thead>
        <tbody class="" align="center">
                <% 
                if(sampledata.length > 0)
                {
                    sampledata.forEach(function(rows){ %>
                        <tr class="">
                            <td class="border-3 border-danger"><%= rows.SrNo %></td>
                            <td class="border-3 border-danger"><%= rows.TeacherId %></td>
                            <td class="border-3 border-danger"><%= rows.TeacherName %></td>
                            <td class="border-3 border-danger"><%= rows.Qualification %></td>
                            <td class="border-3 border-danger"><%= rows.subject %></td>
                        </tr>
                    <% });
                  }
                else 
                {
                    %>
                    <tr ><td colspan="5">No Data Found</td></tr>
            <% } %>
                
        </tbody>
    </table>
    </div>
</body>

</html>